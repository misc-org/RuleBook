---
import 'iconify-icon';

interface Props {
    name: string;
    class?: string;
}

const {name, class: className} = Astro.props;
---

<iconify-icon icon={name} class={`inline-block ${className}`}/>

<script src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"></script>
